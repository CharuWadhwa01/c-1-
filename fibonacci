#include<iostream>
using namespace std;
int fibonaaci(int n)
{
    if (n<2)
    {
        return 1;
    }
    return fibonaaci(n-2)+fibonaaci(n-1);
    
}
int main()
{
    int n;
    cout<<"no is"<<endl;
    cin>>n;
    cout<<"fibonnaci is"<<n<<"is"<<fibonaaci(n)<<endl;
}
